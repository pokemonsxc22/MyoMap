Here's your complete prompt:

```
Make the following upgrades to MyoMap. Use Framer Motion for all animations. Keep all existing functionality intact.

## 1. Landing page — replace social proof with "What our AI looks at" section
Remove the fake testimonials/social proof section and replace it with a section titled "What our AI looks at" showing the inputs MyoMap uses to build your routine. Display as a grid of cards, each with an icon and label:
- 📍 Pain location — where exactly it hurts
- ⏱ Duration — how long you've had the issue
- ⚡ Aggravating factors — what makes it worse
- 🌿 Relieving factors — what makes it better  
- 🏃 Activity level — how active you are
- 🎯 Your goal — reduce pain, improve mobility, return to sport
- 🏋️ Sport/activity — your specific movement demands
- 🔬 Movement screen — functional tests that reveal restrictions
Style as glassmorphism cards with teal (#0D9488) icon accents, fade-in on scroll with Framer Motion whileInView.

## 2. MyoMap name next to logo
- In the navbar, show the square icon logo followed by the text "MyoMap" in white bold font next to it
- Also show "MyoMap" on the landing page hero and auth screens where it makes sense
- Do not repeat it redundantly — just navbar and key entry points

## 3. AI chatbot — fix scroll behavior
- When the user sends a message and the AI responds, do NOT scroll the entire page down
- Only scroll within the chat container itself (the chat box should have its own internal scroll)
- The chat box should have a fixed height with overflow-y: auto so messages scroll inside it, not the page
- This applies to all chatbots in the app (dashboard AI chat, results page AI chat, progress chat)

## 4. AI chatbot — fix markdown rendering
- The AI responses are showing raw markdown like **bold** with asterisks and numbered lists as plain text
- Install react-markdown: npm install react-markdown
- Wrap all AI response text in <ReactMarkdown> so bold, bullet points, numbered lists, and headers render properly
- Apply this fix to every chatbot in the app

## 5. Streak animations and colors
Update the streak display with the following rules based on streak count:
- 1-2 days: standard teal color, no special effect
- 3-4 days: red/orange fire effect 🔥 with red glow (color: #EF4444, glow: 0 0 20px #EF4444)
- 5-6 days: purple effect with purple glow (color: #8B5CF6, glow: 0 0 20px #8B5CF6)
- 7-9 days: blue effect with blue glow (color: #3B82F6, glow: 0 0 20px #3B82F6)
- 10+ days: user can choose their streak color from a color picker with options: red, orange, yellow, green, teal, blue, purple, pink, white, black. The chosen color applies a glow and particle effect
Add floating particle animations around the streak count using Framer Motion when streak is 3+. The particles should match the streak color.

## 6. Streak — weekly scroll with left/right navigation
- Instead of showing only the current week, add left and right arrow buttons to scroll through previous weeks
- Each week shows M T W T F S S with checked/unchecked days
- Animate the week transition left/right with Framer Motion
- Show the date range of the week being viewed (e.g. "Jun 23 – Jun 29")

## 7. Streak — calendar view button
- Add a button below the streak section: "View Calendar"
- Clicking it opens a glassmorphism modal/overlay showing a full month calendar
- Days where the user checked in are highlighted in teal with a glow
- The calendar matches the dark aesthetic with glass styling
- Add an X button to close the modal
- Animate the modal open/close with Framer Motion

## 8. Assessment dropdown — fix styling
- The dropdown menus on the assessments/intake page have a white background with white text making them unreadable
- Fix all dropdowns and select elements to match the dark aesthetic:
  - Background: #1E293B
  - Text color: white
  - Border: 1px solid rgba(13, 148, 136, 0.3)
  - On hover: teal highlight
- If possible, style as glassmorphism dropdowns
- Apply this fix to every select/dropdown in the app

## 9. Results page — clickable exercises with step-by-step instructions
- Make each exercise in the "What to do" section clickable
- When clicked, expand a panel below it (or open a modal) showing step-by-step instructions for how to perform the exercise
- The instructions should be generated by asking the Groq AI for step-by-step form cues for that specific exercise when it is clicked
- Show a loading state while the AI generates the instructions
- Style the expanded panel with glassmorphism, teal accent border, and smooth Framer Motion expand animation

## 10. First-time user onboarding popups
- When a user visits the dashboard for the first time (track this in localStorage as `myomap_dashboard_seen`), show a welcome popup/modal explaining the key features:
  - "Here's how MyoMap works" as the title
  - Explain: Start an assessment → get your routine → use the AI chat → track your progress
  - Style as a glassmorphism modal with teal accents
  - An X button in the top right to close
  - A "Don't show again" button at the bottom that sets `myomap_dashboard_seen = true` in localStorage
  - Animate in with Framer Motion

- When a user visits the results page for the first time (track in localStorage as `myomap_results_seen`), show a popup explaining:
  - "Your personalized routine is ready"
  - Explain: Click any exercise to see how to do it, chat with the AI about your results, save your routine to your dashboard
  - Same glassmorphism style, X button, "Don't show again" button

## 11. Instructions throughout the app
Add small helper text or tooltip icons (ℹ️) next to every major feature explaining what it does:
- Next to "Ask MyoMap AI": "Chat with our AI about your pain, routine, or recovery"
- Next to "Your Streak": "Check in daily to build your streak and stay consistent"
- Next to "My Routines": "Your saved assessment routines — click to expand"
- Next to "Track Your Progress": "Log how each session felt and monitor your improvement over time"
- Next to the movement screen: "These tests help our AI identify your movement restrictions"
These should be subtle — small gray info icons that show a tooltip on hover, not intrusive banners.

## 12. UI consistency
- Make sure every page uses the same dark background (#0a0f1a), glassmorphism cards, teal accents, and Plus Jakarta Sans font
- All dropdowns, selects, and inputs must match the dark theme — no white backgrounds anywhere
- All buttons must be teal — no blue anywhere in the app
```

Paste that into Replit Agent!